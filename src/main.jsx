import React from 'react';
import ReactDOM from 'react-dom/client';
import WarriorsPath from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WarriorsPath />
  </React.StrictMode>
);
